const Button = ({ text, style }: any) => {
	return <button className={`py-2 px-5 bg-[#bc264b] hover:bg-[#e92828] transition duration-500 rounded-lg text-white ${style}`}>مشاهده همه</button>
}

export default Button